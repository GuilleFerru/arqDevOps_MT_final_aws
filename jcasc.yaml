# jenkins:
#   systemMessage: "Jenkins configurado automáticamente por Jenkins Configuration as Code plugin"
#   securityRealm:
#     local:
#       allowsSignup: false
#       users:
#         - id: "admin"
#           password: "123"

# jobs:
#   - script: >
#       multibranchPipelineJob('petclinic-rest') {
#           branchSources {
#               git {
#                   id = 'petclinic-rest'
#                   remote('https://github.com/devops-mitocode/spring-petclinic-rest.git')
#               }
#           }
#       }

jenkins:
  systemMessage: "Jenkins configurado automáticamente por Jenkins Configuration as Code plugin"
  securityRealm:
    local:
      users:
        - id: "admin"
          password: "123"

  # jobs:
  # - script: >
  #     multibranchPipelineJob('arqDevOps_MT_final_monolitica_multibranch') {
  #       branchSources {
  #         github {
  #           id('arqDevOps-MT-final')
  #           scanCredentialsId('')
  #           repoOwner('GuilleFerru')
  #           repository('arqDevOps_MT_final_monolitica')
  #         }
  #       }
  #       orphanedItemStrategy {
  #         discardOldItems {
  #           numToKeep(5)
  #         }
  #       }
  #       triggers {
  #         periodic(5)
  #       }
  #     }
